<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.faisaljaved.myparking">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name="androidx.multidex.MultiDexApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_my_parking_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_my_parking_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme1"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".Activities.MessageActivity"></activity>
        <activity android:name=".Activities.ForgotPasswordActivity"
            android:theme="@style/BlackStatusBar"/>
        <activity android:name=".Activities.SingleAdActivity"
            android:theme="@style/BlackStatusBar"/>
        <activity android:name=".Activities.Settings" />
        <activity android:name=".Activities.ChatsActivity" />
        <activity android:name=".Activities.MyAdsActivity" />
        <activity android:name=".Activities.UserProfileActivity" />
        <activity android:name=".Activities.PostAdActivity" />
        <activity android:name=".Activities.ProfileActivity" />
        <activity android:name=".Activities.SignupActivity"
            android:theme="@style/BlackStatusBar"/>
        <activity android:name=".Activities.LoginActivity"
            android:theme="@style/BlackStatusBar"/>
        <activity android:name=".Activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat"/>
    </application>

</manifest>